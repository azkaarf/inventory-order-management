<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <php>
        <!--
            Bab 10.3: test database terisolasi dari data development.
            Unit tests never touch the DB at all (short-circuit before any
            query runs), so this only actually matters for Integration tests
            - which now hit inventory_test instead of inventory.
        -->
        <env name="DB_NAME" value="inventory_test" force="true"/>
    </php>
</phpunit>
